"""EMPTY until consolidation — do not add logic here before then.

At consolidation, A fills this in: construct B's PhotonMessaging, D's
SqliteProfileStore / VoiceProvider / CalendarProvider / MusicProvider /
MatchingService / ProfileRefresher, and A's MergeRouter / WebVenueSearch /
SqliteArtifactStore; wire them into the Orchestrator. See docs/branch-a.md.
"""


def build_orchestrator():
    raise NotImplementedError("wiring happens at consolidation — run your branch harness instead")
